-- CREAR UN USUARIO
ALTER SESSION SET "_ORACLE_SCRIPT" = TRUE;

CREATE USER algo IDENTIFIED BY algo;
GRANT ALL PRIVILEGES TO algo;


-- CREACION DE TABLAS
CREATE TABLE USUARIO (
    USUARIO VARCHAR2(45) NOT NULL PRIMARY KEY,
    CONTRASENA VARCHAR2(45) NOT NULL
);

CREATE TABLE JUGADOR (
    RUT VARCHAR2(45) NOT NULL PRIMARY KEY,
    NOMBRE VARCHAR2(45) NOT NULL,
    APELLIDO VARCHAR2(45) NOT NULL,
    EDAD NUMBER NOT NULL,
    TIPO VARCHAR2(45) NOT NULL,
    GENERO VARCHAR2(45) NOT NULL,
    TITULAR VARCHAR2(45) NOT NULL
);

INSERT INTO "ALGO"."USUARIO" (USUARIO, CONTRASENA) VALUES ('admin', 'admin')
INSERT INTO "ALGO"."USUARIO" (USUARIO, CONTRASENA) VALUES ('algo', 'algo')
